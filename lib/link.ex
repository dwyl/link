defmodule Link do
  @moduledoc """
  `Link` is a little link parsing, compacting and shortening library.
  """

  @doc """
  `compact/1` reduces a url

  ## Examples

      iex> Link.compact(")
      :world

  """
  def compact(url) do

  end
end
